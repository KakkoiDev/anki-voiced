[project]
name = "anki-voiced"
version = "0.1.0"
description = "Turn any vocabulary list into a professional Anki deck with native-quality audio"
readme = "README.md"
license = { text = "MIT" }
requires-python = ">=3.10"
keywords = ["anki", "flashcards", "tts", "language-learning", "vocabulary"]
classifiers = [
    "Development Status :: 3 - Alpha",
    "Environment :: Console",
    "Intended Audience :: Education",
    "License :: OSI Approved :: MIT License",
    "Programming Language :: Python :: 3",
    "Programming Language :: Python :: 3.10",
    "Programming Language :: Python :: 3.11",
    "Programming Language :: Python :: 3.12",
    "Programming Language :: Python :: 3.13",
    "Topic :: Education",
]
dependencies = [
    "genanki>=0.13.1",
    "kokoro>=0.9.4",
    "soundfile>=0.13.1",
    "typer>=0.15.0",
    "rich>=13.0.0",
    "pydantic>=2.0.0",
]

[project.optional-dependencies]
dev = [
    "pytest>=8.0.0",
    "pytest-cov>=4.0.0",
    "ruff>=0.8.0",
]

[project.scripts]
anki-voiced = "anki_voiced.cli:app"

[project.urls]
Homepage = "https://github.com/kakkoidev/anki-voiced"
Repository = "https://github.com/kakkoidev/anki-voiced"
Issues = "https://github.com/kakkoidev/anki-voiced/issues"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/anki_voiced"]

[tool.ruff]
line-length = 100
target-version = "py310"

[tool.ruff.lint]
select = ["E", "F", "I", "N", "W"]

[tool.pytest.ini_options]
testpaths = ["tests"]
addopts = "-v --cov=anki_voiced"
